# RF驻波比功率计 上位机通信协议规范

> 状态：规划草案，待实现
> 最后更新：2026-03-31

---

## 一、整体通信架构

```
上位机 (PC)
   │
   ├── COM口A → 本产品（RF驻波比功率计）   从机地址 0x01
   └── COM口B → MY12型射频VI功率计         从机地址 0x02
```

上位机通过两个独立串口，使用**统一的 Modbus-RTU 协议**分别与两台设备通信。

### 自动标定工作流程（上位机视角）

```
1. 写本产品 0x0100 = 1          → 开始采零点
2. 轮询本产品 0x0102 == 3       → 等待零点采集完成
3. 控制射频电源输出目标功率（如 50W）
4. 读功率计 0x0002              → 得到实际正向功率（如 45.0W）
5. 写本产品 0x0101 = 450        → 告知本产品参考功率为 45.0W（换算系数 0.1）
6. 写本产品 0x0100 = 2          → 本产品采样当前功率点
7. 轮询本产品 0x0102 == 3       → 等待采样完成
8. 重复步骤 3-7，逐点完成所有标定点（最多20点）
9. 写本产品 0x0100 = 3          → 保存标定数据到 EEPROM
```

---

## 二、本产品通信基础参数

| 参数项 | 值 |
|--------|----|
| 物理接口 | UART（通过 USB-TTL 或 RS232 转接上位机）|
| 波特率 | 115200 |
| 数据位 | 8位 |
| 校验位 | 无 |
| 停止位 | 1位 |
| 核心协议 | Modbus-RTU |
| 默认从机地址 | 0x01 |
| 支持功能码 | 0x03（读保持寄存器）、0x06（写单个寄存器）、0x10（写多个寄存器）|
| 数据字节序 | 大端模式（高字节在前，低字节在后）|
| 最大单次操作寄存器数 | 0x7F（127个）|

---

## 三、Modbus-RTU 帧结构

与 MY12 功率计完全一致，参见功率计协议文档第二章。

### CRC16-MODBUS 校验
- 多项式：0x8005
- 初始值：0xFFFF
- 输出：低字节在前，高字节在后
- 校验范围：地址码 + 功能码 + 数据区

---

## 四、寄存器地址表

### 4.1 测量数据寄存器（只读 R）

| 起始地址 | 寄存器数 | 参数名称 | 数据类型 | 换算系数 | 单位 | 备注 |
|----------|---------|----------|----------|----------|------|------|
| 0x0000 | 2 | 正向功率 | 32位无符号整数 | 0.1 | W | 数值 100 → 10.0W |
| 0x0002 | 2 | 反射功率 | 32位无符号整数 | 0.1 | W | 数值 100 → 10.0W |
| 0x0004 | 1 | 驻波比（VSWR）| 16位无符号整数 | 0.01 | - | 数值 123 → 1.23 |
| 0x0005 | 1 | 回波损耗 | 16位有符号整数 | 0.1 | dB | 数值 -150 → -15.0dB |
| 0x0006 | 2 | 测量频率 | 32位无符号整数 | 1 | Hz | 数值 14000000 → 14MHz |
| 0x0008 | 1 | 标定状态 | 16位无符号整数 | - | - | 0=未标定，1=已标定 |
| 0x0009 | 1 | 数据有效标志 | 16位无符号整数 | - | - | 0=无效，1=有效 |
| 0x000A | 1 | 当前标定点数 | 16位无符号整数 | - | - | 0~20，已录入的标定点数量 |
| 0x000B | 1 | 软件版本号 | 16位无符号整数 | - | - | 示例：0x0100 → V1.00 |

### 4.2 控制/配置寄存器（读写 R/W）

| 起始地址 | 寄存器数 | 参数名称 | 数据类型 | 备注 |
|----------|---------|----------|----------|------|
| 0x0100 | 1 | 标定命令 | 16位无符号整数 | 见下方命令码表 |
| 0x0101 | 2 | 标定参考功率 | 32位无符号整数 | 换算系数 0.1，单位 W；上位机从功率计读取后写入 |
| 0x0102 | 1 | 标定步骤状态 | 16位无符号整数（只读回）| 0=空闲，1=零点采集中，2=功率点采样中，3=步骤完成，4=错误 |
| 0x0103 | 1 | 报警使能 | 16位无符号整数 | 0=关闭，1=开启 |
| 0x0104 | 1 | VSWR报警阈值 | 16位无符号整数 | 换算系数 0.01，数值 300 → 3.00 |
| 0x0105 | 1 | 蜂鸣器使能 | 16位无符号整数 | 0=关闭，1=开启 |
| 0x0106 | 1 | 背光亮度 | 16位无符号整数 | 1~10 |

### 4.3 标定命令码（写入 0x0100）

| 命令值 | 含义 |
|--------|------|
| 0x0001 | 开始零点标定（无信号输入状态下采集零点）|
| 0x0002 | 采样当前功率点（需先写入 0x0101 参考功率）|
| 0x0003 | 保存所有标定数据到 EEPROM |
| 0x0004 | 清除所有标定数据 |
| 0x0005 | 中止当前标定流程 |

---

## 五、异常故障码

| 故障码 | 含义 | 触发原因 |
|--------|------|----------|
| 0x01 | 非法功能码 | 使用了不支持的功能码 |
| 0x02 | 非法数据地址 | 寄存器地址超出有效范围 |
| 0x03 | 非法数据值 | 写入数据超出参数范围 |
| 0x04 | 设备忙 | 标定流程进行中，不接受此操作 |

---

## 六、待确认事项（实现前需敲定）

- [ ] STM32 使用哪个 UART 口对接上位机（UART1 目前是调试口，建议用 UART2）
- [ ] 硬件接口类型：USB-TTL 直接引出，还是加 RS232 电平转换（MAX232）
- [ ] 从机地址 0x01 是否确认（不与功率计 0x02 冲突即可）
- [ ] 标定点数量是否保留 20 点
- [ ] 功率计实际使用频率确认（MY12 仅支持 13.56 / 27.12 / 40.68 MHz）

---

## 七、参考：MY12功率计关键寄存器速查

| 地址 | 参数 | 换算系数 | 备注 |
|------|------|----------|------|
| 0x0002 | 正向功率 | 0.1W | 32位，读2个寄存器 |
| 0x0004 | 反射功率 | 0.1W | 32位，读2个寄存器 |
| 从机地址 | 0x02 | - | 固定 |
| 波特率 | 115200 | - | 默认 |

